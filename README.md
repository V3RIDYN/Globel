# Globel — hint and completion popup version

Upload and replace index.html, style.css, config.js, and script.js in the GitHub repository.

The Show Hint button reads the Optional Hint column. The row must be marked Ready. A completion popup appears after a win or after all six attempts.
